import * as React from 'react';

// eslint-disable-next-line no-unused-vars
function Select(props: React.HTMLAttributes<HTMLInputElement>) {
  return <h1>Select component</h1>;
}

export default Select;
